import express from 'express';

const router = express.Router();

// Placeholder route
router.get('/', (req, res) => {
  res.send('User routes are configured but no specific user endpoints are defined yet.');
});

export default router;
